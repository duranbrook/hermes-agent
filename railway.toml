[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Bootstrap profile, start dashboard on Railway's PORT, then start gateway
startCommand = "bash /opt/hermes/docker/railway-start.sh"
restartPolicyType = "on_failure"

[[services]]
name = "hermes-auto-parts"
