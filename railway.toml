[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Bootstrap profile, start dashboard on Railway's PORT, then start gateway
startCommand = "source /opt/hermes/.venv/bin/activate && bash /opt/hermes/docker/auto-parts-entrypoint.sh && hermes dashboard --host 0.0.0.0 --port ${PORT:-9119} --no-open --insecure & exec hermes gateway run"
restartPolicyType = "on_failure"

[[services]]
name = "hermes-auto-parts"
