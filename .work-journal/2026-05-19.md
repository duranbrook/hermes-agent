# Work Journal: 2026-05-19

## Session 11:00
### What was done
- Session start — no prior journal existed for this project
- Brainstorming session started for a new automotive Hermes profile

### Current state
- Exploring architecture for an automotive-specific Hermes profile
- Key use case: part identification from photo + VIN → part number lookup via WhatsApp
- Phase: brainstorming / design

### Key findings from codebase exploration
- Hermes profiles live at `~/.hermes/profiles/<name>/` — each is a fully isolated HERMES_HOME
- WhatsApp adapter already exists at `gateway/platforms/whatsapp.py` (supports Business API, whatsapp-web.js, Baileys)
- Profile creation: `hermes profile create <name>`
- Skills, tools, config.yaml, .env are all per-profile
- Gateway config controls which platforms are active per-profile

### Next steps
- Complete brainstorming design for automotive profile
- Define: parts DB integration, vision model choice, WhatsApp backend, deployment target

---

## Session (resumed — context compacted)

### What was done
- Completed brainstorming → approved design spec → wrote implementation plan
- **Part A (hermes-auto-parts-api new repo)** — fully implemented and pushed to GitHub:
  - `services/vin_decoder.py` — NHTSA VIN decoder
  - `services/db_adapter.py` — pluggable PartsDB ABC + MockPartsDB stub
  - `routers/parts.py` — POST /lookup endpoint
  - `Dockerfile` + `railway.toml` + `.env.example`
  - Repo at: https://github.com/duranbrook/hermes-auto-parts-api
- **Part B (hermes-agent)** on branch `feat/automotive-profile`:
  - `tools/parts_lookup.py` — HTTP client tool registered with Hermes registry
  - `toolsets.py` — "automotive" toolset added
  - `docker/profiles/auto-parts/SOUL.md` — automotive assistant persona
  - `docker/profiles/auto-parts/config.yaml` — WhatsApp-only profile, reduced toolset
  - `docker/auto-parts-entrypoint.sh` — bootstrap script copies profile files into HERMES_HOME volume on first boot
  - `railway.toml` — Railway deployment config for hermes-auto-parts service

### Current state
- All code is complete and committed
- **BLOCKED on Railway deployment** — `backboard.railway.app` (Railway API) times out from this machine even though `railway.app` CDN is reachable
- Token `a57b1681-5c96-4677-9e6d-a429229eb202` returns "Unauthorized" (may be project-scoped, not personal)
- Stored OAuth tokens in `~/.railway/config.json` expired 2026-05-07

### Key decisions
- Tool auto-discovery only scans `tools/*.py` top-level — `parts_lookup.py` goes there, not `tools/automotive/`
- WhatsApp bridge: repo uses Baileys (not whatsapp-web.js), bridge at `scripts/whatsapp-bridge/bridge.js`
- Profile bootstrap: Railway volumes aren't written at build time, so `auto-parts-entrypoint.sh` copies profile files into `/opt/data/profiles/auto-parts/` on first boot
- GitHub org: `duranbrook` (not `durambrook` — user typo)

### Railway deployment progress (overnight session)
- Railway was down (outage) when first attempted; deployed via GraphQL API after recovery
- Fork created: `duranbrook/hermes-agent` with `feat/automotive-profile` branch
- Both services deployed to Railway project `fabulous-art` (b160f8c6):
  - `hermes-auto-parts-api` — SUCCESS, private, `PARTS_DB_BACKEND=mock`
  - `hermes-auto-parts` — SUCCESS, public at `hermes-auto-parts-production.up.railway.app`
- Railway volume created at `/opt/data` for session persistence
- Fixes applied along the way:
  - Removed Docker `VOLUME` directive (Railway rejects it)
  - Fixed `$PORT` expansion in Dockerfile CMD (used shell form)
  - Removed `healthcheckPath` from gateway railway.toml (bot, not HTTP server)
  - Dashboard port now reads from Railway's `$PORT` via entrypoint.sh
  - Used `serviceConnect` API to redeploy existing service without duplicates

### Next steps — ONE step remaining
- **WhatsApp pairing**: In Railway dashboard → `hermes-auto-parts` → terminal/shell → run `hermes whatsapp` → scan QR → restart service
- After pairing: run final code review + `superpowers:finishing-a-development-branch`
